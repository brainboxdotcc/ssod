// Project: SSOD
// Content: Configuration settings

#define IMAGE_DIR			"/images"	// use "/" for default document root (not recommended)
#define DOCUMENT_DIR			"/var/www/vhosts/ssod.org" // set this to getenv("DOCUMENT_ROOT") to use the document root folder
#define VERSION				"0.99.2 (ANSI)" // version number
#define ADMIN_COOKIE_DIR		"/tmp/"	// location where to store admin-cookies (sessions)
#define DEATH_MESSAGES			53	// number of death messages in the death.msg file
#define IBF_PREFIX			"ssodforums_"	// prefix used by ibf forum (for forum sync with usernames)
